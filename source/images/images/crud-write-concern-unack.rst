
.. only:: latex

   .. figure:: /images//images/crud-write-concern-unack-print.png
      :alt: Write operation to a ``mongod`` instance with write concern of ``unacknowledged``. The client does not wait for any acknowledgment.
      :align: center
      :figwidth: 2100px
   
      Write operation to a ``mongod`` instance with write concern of ``unacknowledged``. The client does not wait for any acknowledgment.


.. only:: website and not html

   .. raw:: html
   
      <div class="figure align-center" style="max-width:300px;"><img src="http://docs.mongodb.org/training/master/_images//images/crud-write-concern-unack.png" alt="Write operation to a ``mongod`` instance with write concern of ``unacknowledged``. The client does not wait for any acknowledgment."></img><p>Write operation to a <tt class="docutils literal">mongod</tt> instance with write concern of <tt class="docutils literal">unacknowledged</tt>. The client does not wait for any acknowledgment.</p></div>


.. only:: website and html

   .. figure:: /images//images/crud-write-concern-unack.png
      :alt: Write operation to a ``mongod`` instance with write concern of ``unacknowledged``. The client does not wait for any acknowledgment.
      :align: center
      :figwidth: 600
   
      Write operation to a ``mongod`` instance with write concern of ``unacknowledged``. The client does not wait for any acknowledgment.

