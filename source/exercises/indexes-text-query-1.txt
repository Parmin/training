Exercise: Querying for a Word
-----------------------------

Using the text index, find all documents in the montyPython collection with the word "swallow" in it.

.. code-block:: javascript

    db.collection.find( { $text : { $search : "swallow" } } )
    // 3 documents
