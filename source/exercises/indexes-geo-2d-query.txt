Exercise: Find Based on 2d Coords
---------------------------------

Write a query to find all documents within 3 degrees of the point [ -2.5, -0.5 ] in the twoD collection.

.. only:: instructor
  
   .. note::

      Answer:

      .. code-block:: javascript
    
         db.twoD.find( { xy : { $geoWithin : { $center : [ [ -2.5, -0.5 ], 3 ] } } } 
